# ===================================================================
# thunderbird-esq .gitignore
# Golden Rule: NEVER commit files with secrets or credentials.
# ===================================================================

# Environment Variables
# The most critical section for security. Contains API keys and secrets.
# We will commit a `.env.example` file to show what variables are needed.
.env
.env*.local
!.env.example
!.env.template

# Dependencies & Lockfiles
# These are installed via package managers and should not be in the repo.
/node_modules
/package-lock.json
/yarn.lock
/pnpm-lock.yaml

# Next.js
# Build output, cache, and exported static sites.
/.next/
/out/

# Supabase
# The Supabase CLI generates a local directory for state and secrets.
# This should NEVER be committed.
/.supabase

# Testing & Coverage
# Generated during testing and not part of the source code.
/coverage
/jest.config.js
/jest.setup.js
/.nyc_output

# Playwright E2E Testing
# Test results, screenshots, videos, and browser artifacts
/test-results/
/playwright-report/
/playwright/.cache/

# Logs
# Log files can contain sensitive information and are specific to a single session.
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Operating System Generated Files
# Useless files generated by macOS, Windows, and Linux.
.DS_Store
.DS_Store?
._*
.AppleDouble
.LSOverride
Thumbs.db
ehthumbs.db
desktop.ini

# Editor Directories and Files
# Personal editor settings.
/.idea
/.vscode/*
# You can UN-ignore specific vscode settings if your team agrees on them
# !/.vscode/settings.json
# !/.vscode/extensions.json
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Miscellaneous
# Compiled TypeScript files (if you're not using a bundler directly)
*.tsbuildinfo
# TurboRepo cache
.turbo
